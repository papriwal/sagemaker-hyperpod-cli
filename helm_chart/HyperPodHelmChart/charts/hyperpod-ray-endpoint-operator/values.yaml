## Namespace for all resources deployed by this chart.
##
namespace: "hyperpod-ray"

## String to partially override chart.fullname template (will maintain the release name)
##
# nameOverride: ""

## String to fully override chart.fullname template
##
# fullnameOverride: ""

## AWS region for resolving regional ECR image registries.
## Required when image.override is not explicitly set.
##
## Region detection priority:
## 1. This explicit region setting (highest priority)
## 2. Global region setting (global.region from parent chart)
##
## Supported regions: ca-central-1, us-east-1, us-east-2, us-west-1, us-west-2,
## eu-west-1, eu-west-2, eu-central-1, eu-north-1, eu-south-2, ap-northeast-1,
## ap-northeast-2, ap-south-1, ap-south-2, ap-southeast-1, ap-southeast-2,
## ap-southeast-3, ap-southeast-4, sa-east-1
region: ""

## Container image configuration
##
image:
  ## Image tag override.
  ## If not specified, defaults to 1.0.173.0_1.0.18.0.
  ##
  # tag: ""

  ## Override the full image URI.
  ## If specified, this overrides the automatic region-based URI selection.
  ## Example: "622623004016.dkr.ecr.us-east-1.amazonaws.com/hyperpod-ray-endpoint-operator:0.1.0"
  ##
  override: ""

## KMS configuration for JWT signing
## When set, both the manager and auth-middleware use AWS KMS HMAC instead of K8s secrets.
##
kmsKeyArn: ""

## Domain for Ray dashboard URLs (e.g., spaces.example.com)
## Required for the extension API and controller to function.
##
domain: ""

## Admin group for webhook annotation protection bypass
##
adminGroup: "system:masters"

## Configure the controller manager deployment
##
manager:
  ## Set to false to skip manager installation
  ##
  enabled: true

  replicas: 1

  ## Enable endpoints by default for all RayClusters (required: "true" or "false")
  ##
  enableEndpointsByDefault: ""

  image:
    pullPolicy: IfNotPresent

  ## Arguments
  ##
  args:
    - --leader-elect
    - --log-file-path=/var/log/aws/clusters/ray-endpoint-operator/hyperpod-ray-endpoint-operator.log
    - --enable-extension-api

  ## Environment variables
  ##
  env:
    - name: NODE_NAME
      valueFrom:
        fieldRef:
          fieldPath: spec.nodeName

  ## Env overrides (--set manager.envOverrides.VAR=value)
  ## Same name in env above: this value takes precedence.
  ##
  envOverrides: {}

  ## Image pull secrets
  ##
  # imagePullSecrets:
  #   - name: myregistrykey

  ## Pod-level security settings
  ##
  podSecurityContext:
    fsGroup: 65532
    runAsGroup: 65532
    runAsNonRoot: true
    runAsUser: 65532
    seccompProfile:
      type: RuntimeDefault

  ## Container-level security settings
  ##
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL
    readOnlyRootFilesystem: true

  ## Resource limits and requests
  ##
  resources:
    limits:
      cpu: 500m
      memory: 128Mi
    requests:
      cpu: 10m
      memory: 64Mi

  ## Manager pod's affinity
  ##
  affinity: {}

  ## Manager pod's node selector
  ##
  nodeSelector:
    kubernetes.io/arch: amd64
    sagemaker.amazonaws.com/compute-type: hyperpod

  ## Manager pod's tolerations
  ##
  tolerations: []

  ## Deployment strategy
  ##
  # strategy:
  #   type: RollingUpdate
  #   rollingUpdate:
  #     maxSurge: 25%
  #     maxUnavailable: 25%

  ## Priority class name
  ##
  # priorityClassName: ""

  ## Topology spread constraints
  ##
  # topologySpreadConstraints: []

  ## Termination grace period seconds
  ##
  terminationGracePeriodSeconds: 10

  ## Custom Deployment labels
  ##
  # labels: {}

  ## Custom Deployment annotations
  ##
  # annotations: {}

  ## Custom Pod labels and annotations
  ##
  # pod:
  #   labels: {}
  #   annotations: {}

  ## Extra volumes and volume mounts
  ##
  extraVolumes: []

  extraVolumeMounts: []

## RBAC configuration
##
rbac:
  ## RBAC resource scope
  ## - false (default): ClusterRole/ClusterRoleBinding (all namespaces)
  ## - true: Role/RoleBinding (release namespace only)
  ##
  namespaced: false

  ## Multi-namespace RBAC role mappings (advanced use)
  ## Maps role suffixes to target namespaces for multi-namespace deployments
  ##
  roleNamespaces:
    "auth-reader": "kube-system"

  ## Helper roles for CRD management (admin/editor/viewer)
  ##
  helpers:
    ## Install convenience admin/editor/viewer roles for CRDs
    ##
    enable: false

## ServiceAccount configuration
##
serviceAccount:
  # Install default ServiceAccount provided
  enable: true

  ## Existing ServiceAccount name (only when enable=false)
  ## Note: When enable=true, respects nameOverride/fullnameOverride
  ##
  # name: ""

  ## Custom ServiceAccount annotations
  ##
  # annotations: {}

  ## Custom ServiceAccount labels
  ##
  # labels: {}

## Custom Resource Definitions
##
crd:
  # Install CRDs with the chart
  enable: true
  # Keep CRDs when uninstalling
  keep: true

## Controller metrics endpoint.
## Enable to expose /metrics endpoint
##
metrics:
  enable: true
  # Metrics server port
  port: 8443
  # Enable secure metrics: HTTPS with certs/auth (true) or HTTP (false).
  # Note: Metrics authn/authz needs ClusterRole access.
  secure: true

## Cert-manager integration for TLS certificates.
## Required for webhook certificates and metrics endpoint certificates.
##
certManager:
  enable: true

## Webhook server configuration
##
webhook:
  enable: true
  # Webhook server port
  port: 9443

## Prometheus ServiceMonitor for metrics scraping.
## Requires prometheus-operator to be installed in the cluster.
##
prometheus:
  enable: false

## Auth-middleware deployment configuration
##
authMiddleware:
  ## Set to false to skip auth-middleware installation
  ##
  enabled: true

  replicas: 2

  image:
    pullPolicy: IfNotPresent

  ## Environment variables
  ##
  env:
    port: "8080"
    jwtIssuer: "hyperpod-ray-endpoint-operator"
    jwtAudience: "hyperpod-ray-endpoint-operator"
    sessionTTL: "6h"
    cookieName: "ray_session"
    cookieSecure: "true"
    cookieSameSite: "lax"
    logFilePath: "/var/log/aws/clusters/ray-endpoint-operator/hyperpod-ray-endpoint-operator-auth-middleware.log"

  ## Resource limits and requests
  ##
  resources:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 100m
      memory: 128Mi

  ## Pod-level security settings
  ##
  podSecurityContext:
    fsGroup: 65532
    runAsGroup: 65532
    runAsNonRoot: true
    runAsUser: 65532
    seccompProfile:
      type: RuntimeDefault

  ## Container-level security settings
  ##
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
      - ALL
    readOnlyRootFilesystem: true

  ## Node selector
  ##
  nodeSelector:
    kubernetes.io/arch: amd64
    sagemaker.amazonaws.com/compute-type: hyperpod

  ## Tolerations
  ##
  tolerations: []

  ## Termination grace period seconds
  ##
  terminationGracePeriodSeconds: 30

